To build you need curl library.
Then simply run "make" command.

by default it compiles with debug flags. If you want optimized binaries, use
make release
if you break the program, run commands:
make clean
make
